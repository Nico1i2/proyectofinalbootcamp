<%= form_with(model: brief, local: true) do |form| %>
  <% if brief.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brief.errors.count, "error") %> prohibited this brief from being saved:</h2>

      <ul>
      <% brief.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :brand_id %>
    <%= form.select :brand, options_from_collection_for_select(@brand, "id", "name")%>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age, id: :brief_age %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :brief_location %>
  </div>

  <div class="field">
    <%= form.label :social_class %>
    <%= form.text_field :social_class, id: :brief_social_class %>
  </div>

  <div class="field">
    <%= form.label :budget %>
    <%= form.number_field :budget, id: :brief_budget %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
