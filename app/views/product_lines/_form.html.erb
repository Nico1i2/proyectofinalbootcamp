<%= form_with(model: product_line, local: true) do |form| %>
  <% if product_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_line.errors.count, "error") %> prohibited this product_line from being saved:</h2>

      <ul>
      <% product_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :product_line_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :product_line_description %>
  </div>

  <div class="field">
    <%= form.label :product_line_ID %>
    <%= form.text_field :product_line_ID, id: :product_line_product_line_ID %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
